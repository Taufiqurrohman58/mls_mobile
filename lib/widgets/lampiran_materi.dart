import 'package:flutter/material.dart';

class LampiranMateri extends StatelessWidget {
  const LampiranMateri({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Lampiran Materi'));
  }
}
